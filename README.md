# 💊 نظام إدارة الأدوية - النسخة التجريبية

تطبيق ويب تفاعلي لعرض واستكشاف بيانات الأدوية باستخدام Streamlit.

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات المطلوبة
```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق
```bash
streamlit run app.py
```

سيفتح التطبيق تلقائيًا في المتصفح على العنوان: `http://localhost:8501`

## 📋 الميزات

### ✅ المتوفر حاليًا:
- 🏠 صفحة رئيسية مع نظرة عامة على قاعدة البيانات
- 💊 عرض جميع الأدوية مع إمكانية البحث والتصفية
- 📋 عرض تفاصيل كاملة لكل دواء
- ➕ إضافة أدوية جديدة
- 🏭 إدارة الشركات المصنعة
- 📊 عرض تقديرات الأوزان حسب العمر
- 📈 إحصائيات وتقارير مرئية

### 🔜 قادم قريبًا:
- استيراد البيانات من ملف Excel تلقائيًا
- تعديل وحذف الأدوية
- رفع صور الأدوية والمنشورات
- طباعة التقارير
- نظام متقدم للبحث

## 📁 بنية الملفات

```
drug/
├── app.py                    # التطبيق الرئيسي
├── database_schema.sql       # مخطط قاعدة البيانات
├── drug_database.db          # قاعدة البيانات (سيتم إنشاؤها تلقائيًا)
├── drug_data.csv            # البيانات المستخرجة من Excel
├── بيانات الادوية.xlsx      # الملف الأصلي
├── requirements.txt          # المكتبات المطلوبة
└── README.md                 # هذا الملف
```

## 💡 نصائح الاستخدام

1. **البحث**: استخدم خانة البحث للعثور على الأدوية بسرعة
2. **التصفية**: استخدم القوائم المنسدلة لتصفية الأدوية حسب الفئة أو التوفر
3. **التفاصيل**: اضغط على دواء معين لعرض جميع المعلومات الطبية والصيدلانية
4. **الإضافة**: استخدم نموذج إضافة الأدوية لإدخال بيانات جديدة

## 🎯 الهدف من هذه النسخة

هذا تطبيق تجريبي (Prototype) للمساعدة في:
- ✅ فهم السيناريو وكيفية عمل التطبيق
- ✅ تجربة عرض البيانات بطرق مختلفة
- ✅ اختبار إضافة وتعديل البيانات
- ✅ تحديد الميزات المطلوبة للنسخة النهائية

## 🛠 التطوير المستقبلي

بعد اختبار هذا النموذج، يمكن تطوير النسخة النهائية باستخدام:
- **PyQt6**: لتطبيق سطح المكتب
- **Flask/Django**: لتطبيق ويب أكثر تقدمًا
- **React + FastAPI**: لتطبيق ويب حديث

## 📝 ملاحظات

- قاعدة البيانات تستخدم SQLite (خفيفة ومناسبة للتجريب)
- جميع البيانات محلية ولا تحتاج إلى خادم
- يمكن نسخ قاعدة البيانات بسهولة لعمل نسخ احتياطية
